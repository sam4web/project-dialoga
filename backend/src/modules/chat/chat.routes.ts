import { Router } from "express";

const chatRouter = Router();

// Get New Chat Contacts
// chatRouter.get("/chats/contacts")

// Get All Ongoing Conversations
// chatRouter.get("/conversations");

// Get Messages for a Specific Conversation
// chatRouter.get("/conversations/:conversationId/messages");

// Get Details of a Conversation
// chatRouter.get("/conversations/:conversationId/detail");

export default chatRouter;
